#include<stdio.h>
int main()
{
    int a,b;
    printf("Enter the value of the length of rectangle:");
    scanf("%d",&a);
    printf("Enter the value of the breadth of rectangle:");
    scanf("%d",&b);
    printf("Area of the reactangle having length %d and breadth %d = %d",a,b,a*b);
    return 0;
}