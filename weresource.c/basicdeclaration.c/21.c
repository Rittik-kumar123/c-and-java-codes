#include<stdio.h>

int main()
{
    int num;
    printf("enter the number:");
    scanf("%d",&num);
    if (num>=0&&num<=20)
    {printf("THe range you entered is [0,20]");}
    else if(num>=21&&num<=40)
    {printf("the range of the number you entered is [21,40]");}
    else{("eroor");}
  
    
}